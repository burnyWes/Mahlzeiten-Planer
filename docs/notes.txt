Notes 

--------------------------------------------------------------------------------

Legende: 
	- = todo
	f = new big feature
	/ = in progress
	x = done 
	m = maybe some day 
	n = not gonna do that 
	b = bug 

--------------------------------------------------------------------------------

TODO-List
--------- 
DONE
n Klickfläche zum abhaken vergrößern
x Firebase haerten: Selbstregistrierung abschalten (Authentication - Settings -
  User actions - "Erstellen (Registrierung) aktivieren" aus). Das Konto besteht,
  die Registrierung wird nicht mehr gebraucht; sonst kann sich mit dem oeffentlichen
  apiKey jeder ein Konto im Projekt anlegen.
x Update-Verhalten des Service Workers pruefen: registerType autoUpdate laedt die App
  beim Update ggf. mitten in der Bedienung neu - mit VoiceOver unangenehm.
x Tab-Leiste oben fixiert
  x Einkaufsliste | Gerichte
x Use case
  x given: Milch bereits auf Einkaufsliste
  x when: Milch hinzufügen
  x then: ein Eintrag Milch 2x auf Liste und bereits existierende Warnung, nicht 2 Einträge
x Gerichte-Seite
  x Liste mit Gerichten (rechts Button "Auf die Einkaufsliste")
  x +-Icon oben rechts um ein Gericht hinzuzufügen
    x Dialog mit Name, Liste von Shopping-Items, Freitexfeld Zutaten, Freitextfeld Rezept
    x Wenn bereits angelegt zusätzlich Löschen-Button
x Design Gerichte-Seite anpassen (Gericht-Butten NICHT füllen, Buttons immer gleiche Breite)
x Beim Tippen von Items Vorschläge aus der Datenbank machen, damit man gleiche Einträge
  nicht ständig neu tippen muss.
x Ein selbst hinzugefügter Artikel erscheint gelegentlich erst nach Sekunden in der
  Liste: die Anzeige zeigt nur, was die Firestore-Momentaufnahme schon gebracht hat.
  Die Zusammenführung rechnet seit MZP-002 Phase 2 mit den eigenen Schreibvorgängen,
  die Anzeige noch nicht. Der E2E-Test "sign in, add, check off and clean up" ist
  deshalb gelegentlich rot, auch schon vor Phase 2.
x Einstellungen
  n Push-Benachrichtigungen
x Vorschläge verwalten: Tippfehler aus dem Artikelkatalog (knownItems) löschen können,
  z. B. unter Einstellungen.
x Später erweitern zu
  x Einkaufsliste | Wochenplaner | Gerichte | Einstellungen
x Wochenplaner
----------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------
TODO 
----------------------------------------------------------------------------------------------------------------------------------

- Alexa-Integration
- PDF-Import
- Firestore-Regeln werden nirgends automatisch ausgerollt: deploy.yml schiebt nur die
  App nach GitHub Pages. Eine neue Sammlung ist deshalb in der Produktion so lange
  gesperrt, bis jemand von Hand
  "npx firebase deploy --only firestore:rules --project mahlzeiten-planer-ecd26"
  aufruft. Entweder in den Workflow aufnehmen (braucht ein Service-Account-Secret)
  oder wenigstens in die Freigabe-Schritte schreiben.
- Datenbank aufraeumen: abgehakte Artikel bleiben dauerhaft in Firestore (items),
  "Aufraeumen" ordnet nur die Ansicht neu. Abgehakte Artikel irgendwann wirklich
  loeschen (z. B. beim Aufraeumen oder nach einer Frist), ohne dass sie dem anderen
  Geraet unter dem Finger verschwinden.
- Firestore-Adapter: die zwei Snapshot-Listener (offen / diese Sitzung abgehakt)
  zu einer or()-Abfrage zusammenfassen. Dann gibt es keinen Zwischenstand mehr,
  in dem die veraltete "offen"-Abfrage die abgehakte ueberschreibt oder der
  Artikel kurz aus beiden Abfragen faellt. Seit MZP-006 faengt die optimistische
  Anzeige das ab, die Momentaufnahme bleibt aber kurzzeitig in sich
  widerspruechlich.
- Firestore mit persistentLocalCache (IndexedDB) einrichten: ausstehende
  Schreibvorgaenge ueberleben heute kein Neuladen. Wer einen Artikel hinzufuegt und
  die App sofort neu laedt oder schliesst, verliert ihn stillschweigend - seit
  MZP-006 sichtbar, weil die Zeile sofort erscheint und nicht erst nach der
  Bestaetigung. Der E2E-Test "keeps the added item after a reload" wartet deshalb
  auf den Server, bevor er neu laedt.
- Eisfach-Memory
b test/domainLayerBoundary.test.ts laeuft gelegentlich in die 5000-ms-Grenze von
  vitest, wenn alle 21 Testdateien parallel laufen - der Test startet ESLint
  programmatisch. Allein braucht er 2,9 s. Einmal gesehen am 2026-09-18; der
  Wiederholungslauf war gruen. Entweder testTimeout fuer diese Datei hochsetzen
  oder die ESLint-Instanz ueber die Tests hinweg wiederverwenden.
- Gericht im Wochenplan ausblenden können
- Menge auf Einkaufsliste anpassbar machen
b Wochenplan: useWeekPlan uebernimmt jede Momentaufnahme ungeprueft in den Zustand, ohne
  Schutz fuer noch unbestaetigte eigene Schreibvorgaenge - anders als die Einkaufsliste,
  die das seit MZP-006 ueber unconfirmedWrites abfaengt. Einmal gesehen am 2026-09-19 im
  E2E-Lauf: nach "Zufallsauswahl generieren" stand die Ueberschrift auf "7 von 7",
  uebergeben wurde an die Einkaufsliste aber nur ein Tag. Der Wiederholungslauf war
  gruen, und im selben Lauf war auch der bekannt wackelige Test "sign in, add, check off
  and clean up" rot. Der Test wartet jetzt vor der Uebergabe auf den Server; die Ursache
  ist damit nicht behoben, nur aus dem Test herausgehalten.
- Kategorien in Gerichten
- Kategorie-Verwaltung in Einstellungen als dritte Option
- 3 Zeilen pro Tag im Wochenplan
- Button-Farben anpassen
- Mengenpicker auf Einkaufsliste
