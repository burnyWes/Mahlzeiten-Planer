Notes 

--------------------------------------------------------------------------------

Legende: 
	- = todo
	f = new big feature
	/ = in progress
	x = done 
	m = maybe some day 
	n = not gonna do that 
	b = bug 

--------------------------------------------------------------------------------

TODO-List
--------- 
DONE 
x Firebase haerten: Selbstregistrierung abschalten (Authentication - Settings -
  User actions - "Erstellen (Registrierung) aktivieren" aus). Das Konto besteht,
  die Registrierung wird nicht mehr gebraucht; sonst kann sich mit dem oeffentlichen
  apiKey jeder ein Konto im Projekt anlegen.
x Update-Verhalten des Service Workers pruefen: registerType autoUpdate laedt die App
  beim Update ggf. mitten in der Bedienung neu - mit VoiceOver unangenehm.
x Tab-Leiste oben fixiert
  x Einkaufsliste | Gerichte
x Use case
  x given: Milch bereits auf Einkaufsliste
  x when: Milch hinzufügen
  x then: ein Eintrag Milch 2x auf Liste und bereits existierende Warnung, nicht 2 Einträge
x Gerichte-Seite
  x Liste mit Gerichten (rechts Button "Auf die Einkaufsliste")
  x +-Icon oben rechts um ein Gericht hinzuzufügen
    x Dialog mit Name, Liste von Shopping-Items, Freitexfeld Zutaten, Freitextfeld Rezept
    x Wenn bereits angelegt zusätzlich Löschen-Button
----------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------
TODO 
----------------------------------------------------------------------------------------------------------------------------------

- Später erweitern zu
  - Einkaufsliste | Wochenplaner | Gerichte | Einstellungen
- Wochenplaner
- Einstellungen
  - Push-Benachrichtigungen
- Alexa-Integration
- PDF-Import
b Ein selbst hinzugefügter Artikel erscheint gelegentlich erst nach Sekunden in der
  Liste: die Anzeige zeigt nur, was die Firestore-Momentaufnahme schon gebracht hat.
  Die Zusammenführung rechnet seit MZP-002 Phase 2 mit den eigenen Schreibvorgängen,
  die Anzeige noch nicht. Der E2E-Test "sign in, add, check off and clean up" ist
  deshalb gelegentlich rot, auch schon vor Phase 2.
- Firestore-Regeln werden nirgends automatisch ausgerollt: deploy.yml schiebt nur die
  App nach GitHub Pages. Eine neue Sammlung ist deshalb in der Produktion so lange
  gesperrt, bis jemand von Hand
  "npx firebase deploy --only firestore:rules --project mahlzeiten-planer-ecd26"
  aufruft. Entweder in den Workflow aufnehmen (braucht ein Service-Account-Secret)
  oder wenigstens in die Freigabe-Schritte schreiben.
- Design Gerichte-Seite anpassen (Gericht-Butten NICHT füllen, Buttons immer gleiche Breite)
